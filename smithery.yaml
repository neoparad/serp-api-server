# Smithery.ai configuration
startCommand:
  type: stdio
  configSchema: {}
  commandFunction: |-
    (config) => ({
      "command": "node",
      "args": [
        "src/server.js"
      ],
      "env": {}
    })
